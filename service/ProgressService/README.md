# ProgressService Module

This folder contains the ProgressService module for all progress-related API calls.

## Structure
- `index.ts`: Main service class for progress APIs (get daily, upsert daily, get overview)
- Add any helpers, utils, or types for progress features in this folder.

## Usage
Import from:
```ts
import { progressService } from "@/service/ProgressService";
```
